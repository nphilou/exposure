# Local development (npm run dev). Docker users: see docker-compose.yml
# Folder that the "Found on this server" source browses
EXPOSURE_LOCAL_ROOT=./photos
# Index DB + thumbnail cache + saved connection
EXPOSURE_DATA_DIR=./data
PORT=8787
# Optional shared password. If set, the whole app requires login.
EXPOSURE_PASSWORD=
